sudo: false
language: android
jdk:
  - oraclejdk8
android:
  components:
    - tools
    - platform-tools
    - build-tools-28.0.3
    - android-28

    - extra-android-m2repository

script: cd Clover && ./gradlew build --console plain -x lint

notifications:
  email: false
